{% extends "main.jinja" %}
{% block content %}
<form method="post" action="../tokens">
  <div class="container">
    <h1>Liste de Post</h1>
    <br>
    <hr>
    <br><br>
        <h3><b> {{ post.titre }} </b></h3>
        <ul>
            <img src="{{ post.Image }}">
            <br>
            <li> {{ post.corpus }}</li>
            <br>
            <i> Date de création : {{ post.datecree }} <br>
              Date de modification : {{ post.datemodif }}</i>
            <br>
        </ul>
        {% for tag in post.tags %}
        <span>{{ tag.titre }}</span>
        {% endfor %}
    <hr>

  </div>
</form>

</style>
{% endblock %}